/*package edu.harbourspace.university.matchingengine;

import org.junit.jupiter.api.Test;

import static org.mockito.BDDMockito.given;
import static org.mockito.Mockito.mock;

public class MatchingEngineTest {
    private final InputReader reader = mock(InputReader.class);
    private final MatchingEngine matchingEngine = new MatchingEngine(reader, );

    @Test
    public void testMatchingEngine() {
        given(reader.readMaxPosition()).willReturn(5_000_000L);
        given(reader.readOrder()).willReturn("DF ID1 BUY 100 45.0 BEX");

        matchingEngine.run();
    }
}*/
